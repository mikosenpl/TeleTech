import MainTemplate from '../../common/components/MainTemplate/MainTemplate';
import TelevisionOffers from '../../common/components/TelevisionOffers/TelevisionOffers';

const TelevisionOffersPage = () => {
  return (
    <MainTemplate>
      <TelevisionOffers />
    </MainTemplate>
  );
};

export default TelevisionOffersPage;
