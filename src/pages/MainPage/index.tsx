import MainContent from '../../common/components/MainContent/MainContent';
import MainTemplate from '../../common/components/MainTemplate/MainTemplate';

const MainPage = () => {
  return (
    <MainTemplate>
      <MainContent />
    </MainTemplate>
  );
};

export default MainPage;
